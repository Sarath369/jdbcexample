CREATE TABLE customer_detail (cd_id INT UNSIGNED PRIMARY KEY UNIQUE NOT NULL AUTO_INCREMENT, cust_id INT UNSIGNED NOT NULL, FOREIGN KEY (cust_id) REFERENCES customer(id), city_name VARCHAR(100) NOT NULL, town_name VARCHAR(100) NOT NULL, address1 VARCHAR(100) NOT NULL, address2 VARCHAR(100),  pincode INT NOT NULL);